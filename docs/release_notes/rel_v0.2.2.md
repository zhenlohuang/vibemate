## v0.2.2

### Highlights
- Updated default `init` config to avoid forcing a local proxy.
- Unified Dashboard terminology from “API Proxy” to “API Router”.

### Changes
- fix(config): comment out default `[system].proxy` in generated config template.
- fix(dashboard): rename Usage/Status labels from `API Proxy` to `API Router` for consistency.

**Full Changelog**: [v0.2.1...v0.2.2](https://github.com/zhenlohuang/vibemate/compare/v0.2.1...v0.2.2)
